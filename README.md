eslint will run automatically on each precommit
to run it manualy:
 - lint:server
 - lint:client

on prepush will run test automatically
to run it manualy:
 - npm run test:server
 - npm run test:client

to run server:
 - npm run dev
